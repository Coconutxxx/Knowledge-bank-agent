1、管理员创建流程：
    只能通过create_user.py脚本创建管理员账号，且只能通过该脚本创建第一个管理员账号。
    后续如果需要添加其他管理员，流程为：
        现有管理员登录系统
        → 打开用户管理页面
        → 选择已有用户
        → 将角色调整为 admin

2、用户创建流程：
    只能通过create_user.py脚本创建普通用户，管理员账号不能通过本脚本创建。
    运行脚本后，输入用户名、密码和选择角色即可创建成功。

3、用户删除流程：
    启用用户
    → 管理员先停用
    → 进入“删除用户”
    → 选择用户
    → 手动输入用户名确认
    → 勾选永久删除
    → 删除成功并刷新用户列表